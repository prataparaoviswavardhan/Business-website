-This is a basic frontend prototype website for a healthcare service built using HTML and CSS.

-The project was created to practise frontend web development concepts such as layouts, styling, navigation, sections, and responsive design.


#NOTE

-I used base64 images instead of connecting the actual image because i wanted to test how image embedding will turn out to be.

And in future i will prioritize using normal images and linking them using <img src>

- During development in VS Code, the project was separated into two files:
  - `index.html`
  - `styles.css`
- For this repository, both files were merged into a single HTML file.

#FUTURE COMMITS

a)I will separate the files back to two files and link them using <link>

b) I want to add more features to the home page including adding more images 

c)Using JS to increse interactivity
